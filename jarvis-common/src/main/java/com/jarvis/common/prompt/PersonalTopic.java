package com.jarvis.common.prompt;

/**
 * Personal topic detected in a user request.
 */
public enum PersonalTopic {
    /** User hardware and devices. */
    DEVICE,
    /** User vehicle. */
    VEHICLE,
    /** User projects. */
    PROJECT,
    /** User work. */
    WORK,
    /** User preferences. */
    PREFERENCE,
    /** User identity. */
    IDENTITY,
    /** Other personal topic. */
    OTHER
}
