package com.jarvis.common.prompt;

/**
 * Response mode selected for prompt and validation policy.
 */
public enum ResponseMode {
    /** Standard assistant response mode. */
    STANDARD,
    /** Strictly grounded personal response mode. */
    GROUNDED_PERSONAL
}
